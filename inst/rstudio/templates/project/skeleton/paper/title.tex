% Custom title-block partial for the qkit paper template.
% Activated via "template-partials: title.tex" in the YAML header.
%
% Renders:
%   \title{<YAML title>\\[0.3cm] {\normalsize\itshape Preliminary draft ...}}
%   \author{<each YAML author with ORCID superscript and \thanks{} for email+address>}
%   \date{<YAML date>}
%
% Structured authors expected in the YAML:
%
%   author:
%     - name: Author One
%       orcid: 0000-0000-0000-0001
%       email: author.one@example.org
%       affiliations:
%         - name: Department, University, City, Country
%     - name: Author Two
%       ...
%     - name: Author Three
%       corresponding: true
%       ...

$if(title)$
\title{$title$$if(thanks)$\thanks{$thanks$}$endif$\\[0.3cm]%
{\normalsize\itshape Preliminary draft --- please do not cite without permission}%
}
$endif$

$if(by-author)$
\author{%
$for(by-author)$%
$it.name.literal$%
$if(it.orcid)$\textsuperscript{\normalsize\orcidlink{$it.orcid$}}$endif$%
\thanks{%
$if(it.email)$Email: \href{mailto:$it.email$}{$it.email$}.$endif$%
$if(it.corresponding)$ Corresponding author.$endif$%
$if(it.affiliations)$$for(it.affiliations)$ Address: $it.name$.$endfor$$endif$%
}%
$if(it.is-last)$$else$\nolinebreak[4]$if(it.is-first)$$if(it.next-is-last)$ and $else$~, $endif$$else$$if(it.next-is-last)$~, and $else$~, $endif$$endif$$endif$%
$endfor$%
}
$endif$

$if(date)$
\date{$date$}
$endif$
