$if(has-frontmatter)$
    \frontmatter
$endif$
$if(title)$
\cleardoublepage
\thispagestyle{empty}
{\centering
    \hbox{}\vskip 0cm plus 1fill
    {\Huge\bfseries $title$ \par}
    $if(subtitle)$
        \vspace{3ex}
        {\Large\bfseries $subtitle$ \par}
    $endif$
        \vspace{12ex}
    $for(by-author)$
        {\Large\bfseries $by-author.name.literal$ \par}
        $if(by-author.orcid)$
            \vspace{1ex}
            {\Large ORCID: $by-author.orcid$ \par}
        $endif$
        $for(by-author.affiliations)$%
            \vspace{2ex}
            $if(it.department)$%
                {\large $it.department$ \par}
                \vspace{1ex}
            $endif$%
            {\large $it.name$ \par}
        $endfor$%
        \vspace{3ex}
    $endfor$
    \vskip 0cm plus 2fill
    $if(date)$
        {\bfseries\large $date$ \par}
    $endif$
}
$endif$

% Optional dedication page. Renders only when YAML metadata includes
% `dedication-to:`; if `dedication-text:` is also set, it appears as a
% smaller line below the dedicatee's name. The page starts on a fresh
% recto via \cleardoublepage so it falls right after the title page.
$if(dedication-to)$
\cleardoublepage
\thispagestyle{empty}
\null\vfill
\begin{center}
    {\Large To $dedication-to$,\par}
    $if(dedication-text)$
        \vspace{2ex}
        {\large $dedication-text$\par}
    $endif$
\end{center}
\vfill\null
$endif$
